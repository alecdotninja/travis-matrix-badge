{
  "name": "travis-matrix-badge",
  "version": "1.0.0",
  "description": "netlify functions:create - default template for auth",
  "main": "badge.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.3.0"
  }
}
